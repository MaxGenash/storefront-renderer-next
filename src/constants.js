const PARSING_TIMEOUT = 1000;
const EXECUTION_TIMEOUT = 5000;
const NEXT_SERVER_FILE_PATH = require.resolve('./nextServer.js');

module.exports = {
    PARSING_TIMEOUT,
    EXECUTION_TIMEOUT,
    NEXT_SERVER_FILE_PATH,
};
